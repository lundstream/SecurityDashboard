version: "3.8"
services:
  securitydashboard:
    build: .
    image: lundstream/securitydashboard:latest
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
